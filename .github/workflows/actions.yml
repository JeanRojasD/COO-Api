name: actions test
on:
  push:
    branches:
      - actions
jobs:
  build:

    name: (╬▔皿▔)╯ Unit Test

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Test Action
        run: BrandTest.java

