name: actions test
on:
  push:
    paths:
      - '**\**\src\test\java\com\br\cooapi\brand'
      branches:
        - actions
jobs:
  build:

    name: (╬▔皿▔)╯ Unit Test

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Test Action
        run: BrandTest.java
