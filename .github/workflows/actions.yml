name: actions test
on:
  push:
    branches:
      - actions
jobs:
  build-teste:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Test Action
        uses: drudzikatlassian/test-action@v0.0.1-alpha
