name: actions test
on:
  push:
      branches:
        [ actions ]
jobs:
  build:
    name: (╬▔皿▔)╯ Unit Test

    runs-on: ubuntu-latest

    steps:
      - name: Chekout code
      - uses: actions/checkout@v2

      - name: Test Action
        run: ../../src/test/java/com/br/cooapi/brand BrandTest.java
